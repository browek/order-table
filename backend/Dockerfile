FROM anapsix/alpine-java
ENV ARTIFACT_NAME=order-table-0.0.1-SNAPSHOT.jar
ENV APP_HOME=/app
WORKDIR $APP_HOME
COPY ./build/libs/$ARTIFACT_NAME $APP_HOME/
EXPOSE 8080
ENTRYPOINT java -jar $APP_HOME/$ARTIFACT_NAME